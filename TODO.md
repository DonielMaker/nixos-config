# TODO
- Instead of using a settings.nix in all of the hosts we would like the dynamic configuration to be defined in either 
configuration.nix or in home.nix. This way we don't have a file that doesn't follow the nixos module standard (Right now it is just a .nix file).
